//its a good approach to import all the components in index file and then export the index file so that other file can access the components from it.

import InputBox from "./InputBox";

export {InputBox}
